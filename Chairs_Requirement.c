#include <stdio.h>
int main ( )
{
    int X,Y;
    scanf("%d%d",&X,&Y);
    if (X>Y)
    {
        printf("%d",X-Y);
    }
    else if (X=Y)
    {
        printf("%d",0);
    }
    else 
    {
        printf("%d",0);
    }
}